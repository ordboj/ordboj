dist
coverage
node_modules
package-lock.json
src/components/ui

# verbData.ts uses a deliberate one-object-per-line layout; qa's source-text
# parser in verbData.test.ts matches `infinitive: "..."` and `grupp:` on the
# same line. Prettier's multi-line object formatting breaks that parser.
src/data/verbData.ts
