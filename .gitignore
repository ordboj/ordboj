# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage output
coverage

# Playwright e2e output
test-results
playwright-report
blob-report
playwright/.cache

# Claude Code local machine settings
.claude/settings.local.json

# Workflow worktrees (session-local)
.claude/worktrees/

# Regenerated by `npm run build:verb-data` (scripts/build-verb-data.mjs).
# Not committed: nothing diffs the committed copy against a fresh run, so a
# checked-in snapshot goes silently stale the next time the CSV changes.
# Regenerate locally when needed; --check mode (the CI job) never writes it.
scripts/verb-data-review.csv
docs/verb-data/proposed-rows.txt
